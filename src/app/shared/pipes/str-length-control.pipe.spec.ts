import { StrLengthControlPipe } from './str-length-control.pipe';

describe('StrLengthControlPipe', () => {
  it('create an instance', () => {
    const pipe = new StrLengthControlPipe();
    expect(pipe).toBeTruthy();
  });
});
